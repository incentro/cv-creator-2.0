import React from "react"

const Login = () => (
  <div className="loginbox">
    Let's create your CV!
    <button className="btn btn--orange">Login with Incentro account</button>
  </div>
)

export default Login